# 生猪日度数据面板

牧原 / 温氏 / 德康 三家企业日度成交数据监控面板。

- `index.html` — 页面（布局、图表、环比与累计计算）
- `data.js` — 数据文件（唯一需要更新的文件）

## 更新数据

把数据（Excel / CSV / 文本 / 截图）发给 Hermes Agent，由 agent 解析后覆写 `data.js` 并提交，
线上页面自动刷新，网址不变。

## 本地预览

直接双击 `index.html` 即可（无需服务器）。
